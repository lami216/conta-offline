import { parseAndValidateBackup,restoreNativeBackup } from "../../../../lib/backup.ts";import { validSameOrigin } from "../../../../lib/auth.ts";export async function POST(r:Request){if(!validSameOrigin(r))return Response.json({error:"طلب غير صالح"},{status:403});try{restoreNativeBackup(parseAndValidateBackup(await r.text()));return Response.json({restored:true})}catch(e){return Response.json({error:e instanceof Error?e.message:"تعذرت الاستعادة"},{status:400})}}
